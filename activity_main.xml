package com.example.inventoryappmorgunleonard1;

import android.os.Bundle;
import android.widget.Button;
import android.widget.EditText;
import android.widget.Toast;

import androidx.appcompat.app.AppCompatActivity;

public class AddItemActivity extends AppCompatActivity {

    private DBHelper dbHelper;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_add_item);

        dbHelper = new DBHelper(this);

        EditText editItemName = findViewById(R.id.editItemName);
        EditText editItemQty = findViewById(R.id.editItemQty);
        Button buttonSave = findViewById(R.id.buttonSaveItem);
        Button buttonCancel = findViewById(R.id.buttonCancel);

        buttonSave.setOnClickListener(v -> {
            String name = (editItemName.getText() == null) ? "" : editItemName.getText().toString().trim();
            String qtyStr = (editItemQty.getText() == null) ? "" : editItemQty.getText().toString().trim();

            if (name.isEmpty() || qtyStr.isEmpty()) {
                Toast.makeText(this, "Enter an item name and quantity.", Toast.LENGTH_SHORT).show();
                return;
            }

            int qty;
            try {
                qty = Integer.parseInt(qtyStr);
            } catch (NumberFormatException e) {
                Toast.makeText(this, "Quantity must be a number.", Toast.LENGTH_SHORT).show();
                return;
            }

            long id = dbHelper.addItem(name, qty);
            if (id != -1) {
                Toast.makeText(this, "Item added", Toast.LENGTH_SHORT).show();
                finish(); // return to Inventory
            } else {
                Toast.makeText(this, "Error adding item", Toast.LENGTH_SHORT).show();
            }
        });

        buttonCancel.setOnClickListener(v -> finish());
    }
}
